{
  id: @user.id.to_s,
  email: @user_credential.email_address,
  human_id: @user.human_id,
  authenticated: @user.authenticated?,
  success: true,
  data: {
    user: @user.as_json_for_api,
    token: @jwt_token
  }
}
